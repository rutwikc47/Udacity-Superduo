<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/abprim</item>
        <item name="colorPrimaryDark">@color/sbdark</item>

        <item name="homeAsUpIndicator">@drawable/ic_navdrawer</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_navdrawer</item>

        <!-- Customize your theme here. -->
    </style>

</resources>
